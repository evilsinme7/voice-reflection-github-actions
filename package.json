{
  "name": "voice-reflection-github-actions",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Serverless voice reflection workflow powered by GitHub Actions, DeepSeek and Notion",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check scripts/capture.mjs && node --check scripts/weekly-review.mjs"
  }
}
